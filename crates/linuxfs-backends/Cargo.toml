[package]
name = "linuxfs-backends"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
linuxfs-core = { path = "../linuxfs-core" }
linuxfs-ext = { path = "../linuxfs-ext" }
linuxfs-squashfs = { path = "../linuxfs-squashfs" }
linuxfs-xfs = { path = "../linuxfs-xfs" }

[lints]
workspace = true
